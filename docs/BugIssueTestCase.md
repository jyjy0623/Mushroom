# Bug Issue 测试用例文档

> 记录各版本修复缺陷的 UI 验证测试用例及预期结果。
> 每次修复后在真机/模拟器上逐项验证，确认通过后打勾。

---

## v1.5.0（待发版） — 新建任务日期感知 & 历史页禁止新建

**发布日期**：待定
**关联 Issue**：#33
**修复范围**：
- #33：`DailyTaskListScreen` 历史日期（过去）隐藏新建 FAB；主 FAB 点击时将当前浏览日期传入路由；`AppDestination.TaskEdit` 增加 `date` 路由参数；`TaskEditScreen` 新增「任务日期」字段（默认=传入日期，仅新建可改，不可选过去日期）

---

#### TC-B21 历史日期页不显示新建任务 FAB

**前置条件**：
- 当前处于今天的任务列表页

**操作步骤**：
1. 点击 TopBar 左箭头（←），切换到昨天（历史日期）
2. 观察右下角 FAB 按钮

**预期结果**：
1. 切换到昨天后，右下角「+」FAB 按钮消失（不可见）
2. 展开菜单中的「模板」「复制」「添加里程碑」次级按钮不可触发（FAB 已隐藏）
3. 切换回今天或明天，FAB 重新出现

- [ ] 通过
> 不通过时记录：
> - 实际结果：
> - 设备 / Android 版本 / App 版本：
> - 截图或日志：（可选）
> - Issue：#33

---

#### TC-B22 从明天页面进入新建任务，默认日期为明天

**前置条件**：
- 当前处于今天的任务列表页

**操作步骤**：
1. 点击 TopBar 右箭头（→），切换到明天的任务列表
2. 点击右下角「+」FAB → 展开后点击主按钮进入新建任务页
3. 观察「任务日期」字段显示的日期

**预期结果**：
1. 新建任务页顶部「任务日期」字段显示明天的日期（格式：yyyy年MM月dd日 周X）
2. 字段旁有「修改」按钮
3. 保存后，该任务出现在明天的任务列表中，而非今天

- [ ] 通过
> 不通过时记录：
> - 实际结果：
> - 设备 / Android 版本 / App 版本：
> - 截图或日志：（可选）
> - Issue：#33

---

#### TC-B23 新建任务页可点击「修改」更改任务日期，不可选过去日期

**前置条件**：
- 处于新建任务页（从今天列表进入，默认日期为今天）

**操作步骤**：
1. 观察「任务日期」字段及「修改」按钮
2. 点击「修改」按钮，弹出日期选择器
3. 尝试选择昨天（过去日期）后点击「确定」
4. 重新选择明天后点击「确定」

**预期结果**：
1. 「任务日期」字段显示当前选中日期
2. 点击「修改」弹出 DatePickerDialog
3. 选择昨天点击确定后，日期**不**更新（过去日期不允许选择）
4. 选择明天点击确定后，日期更新为明天
5. 保存后，任务出现在明天的任务列表

- [ ] 通过
> 不通过时记录：
> - 实际结果：
> - 设备 / Android 版本 / App 版本：
> - 截图或日志：（可选）
> - Issue：#33

---

**发布日期**：待定
**关联 Issue**：#22、#23
**修复范围**：
- #22：`RewardListScreen.RewardCard` 图标 fontSize 改为 `32.sp`（统一 DSL 写法），消除 🎁/⏱ 视觉大小差异
- #23：`RewardDetailScreen.ExchangeSection` 将 `OutlinedButton(Modifier.size(48dp))` 改为自定义 `Box+border` 圆形按钮，修复 emoji 被内部 padding 裁剪为空白的问题；新增兑换条件文案行（展示 `reward.requiredMushrooms`）

---

#### TC-B22 奖品列表卡片实物型与时长型图标大小一致

**前置条件**：
- 奖品列表中同时有实物型（🎁）和时长型（⏱）奖品各至少 1 个

**操作步骤**：
1. 进入「奖品」列表页
2. 目视比较两种类型奖品卡片顶部图标的视觉大小

**预期结果**：
1. 两种图标在卡片内占位区域视觉大小相当（均为 32.sp）
2. 图标居中对齐，无偏移

- [ ] 通过
> 不通过时记录：
> - 实际结果：
> - 设备 / Android 版本 / App 版本：
> - 截图或日志：（可选）
> - Issue：#22

---

#### TC-B23 奖品详情「消耗蘑菇」区域正确显示 emoji 按钮与兑换条件

**前置条件**：
- 已创建「高达模型」（兑换条件：✨ 金蘑菇×1）和「看电视」（兑换条件：🍄 小蘑菇×3）

**操作步骤**：
1. 进入「高达模型」详情页，下滑到「消耗蘑菇」卡片
2. 观察五个等级按钮内容及兑换条件文案
3. 点击其中一个按钮，确认选中状态高亮

**预期结果**：
1. 五个按钮分别显示 🍄 🍄‍🟫 🌟 ✨ 👑，不再是空白圆圈
2. 按钮上方显示「兑换条件：✨ 金蘑菇 × 1」文案
3. 点击按钮后，对应按钮边框变为主题色，背景淡蓝
4. 「看电视」详情页同样显示「兑换条件：🍄 小蘑菇 × 3」

- [ ] 通过
> 不通过时记录：
> - 实际结果：
> - 设备 / Android 版本 / App 版本：
> - 截图或日志：（可选）
> - Issue：#23

---

## v1.4.2（待发版） — 数字输入框清空修复 & 时长奖品卡片默认进度

**发布日期**：待定
**关联 Issue**：#18、#19
**关联提交**：待填入
**修复范围**：
- #18：`RewardCreateUiState` 数字字段改为 `String` 类型，`onValueChange` 直接透传字符串，`save()` 时才做 `toIntOrNull()` 转换与校验，彻底解决退格遗留"1"问题
- #19：`RewardListViewModel` / `RewardDetailViewModel` 当 `getTimeRewardBalanceUseCase` 返回 null 时，用 `timeLimitConfig.maxMinutesPerPeriod` 构造默认 `TimeRewardBalance(usedMinutes=0)`，确保时长型奖品卡片始终显示进度信息

---

#### TC-B18 数字输入框可完全清空（退格无遗留）

**前置条件**：
- 处于「创建奖品」页面

**操作步骤**：
1. 在「兑换所需蘑菇」区域找到「×数量」输入框（默认值 1）
2. 点击输入框，全选内容，按退格键清空
3. 重新输入「3」

**预期结果**：
1. 退格后输入框显示空字符串（不会弹回"1"）
2. 重新输入「3」后显示「3」
3. 对「拼图总块数」「每次兑换获得（分钟）」「周期最多使用（分钟）」「冷却天数」均同样无遗留问题

- [ ] 通过
> 不通过时记录：
> - 实际结果：
> - 设备 / Android 版本 / App 版本：
> - 截图或日志：（可选）
> - Issue：#18

---

#### TC-B19 时长型奖品卡片创建后立即显示「0/N 分钟」

**前置条件**：
- 已按 TC-2.5.1-02 创建「看电视」时长型奖品（周期上限 90 分钟，从未兑换）

**操作步骤**：
1. 保存后返回奖品列表
2. 找到「看电视」卡片，观察卡片底部进度信息

**预期结果**：
1. 卡片显示「0/90 分钟」
2. 进度条为空（0%）
3. 不崩溃，不显示"无进度"占位

- [ ] 通过
> 不通过时记录：
> - 实际结果：
> - 设备 / Android 版本 / App 版本：
> - 截图或日志：（可选）
> - Issue：#19

---

## v1.4.1（待发版） — 蘑菇余额全显示 & 全勤奖触发 & 今日进度里程碑提示

**发布日期**：待定
**关联 Issue**：#15、#16、#17
**关联提交**：待填入
**修复范围**：
- #15：`MushroomLedgerScreen.BalanceCard` 移除余额为 0 不显示的过滤，五个等级始终全部展示
- #16：`CheckInTaskUseCase` 打卡后检查当日全部完成并额外触发全勤奖事件（`taskId=-1L`）；`TaskProgressCard` 新增全勤奖提示文案
- #17：`DailyTaskViewModel` 注入 `CheckInRepository` 加载连续打卡天数；`TaskProgressCard` 新增连续天数及下一里程碑提示

---

#### TC-B15 蘑菇账本余额卡显示全部五个等级（含余额为 0 的等级）

**前置条件**：
- 学生账户：小蘑菇 2、中蘑菇 0、大蘑菇 0、金蘑菇 0、传说蘑菇 0

**操作步骤**：
1. 进入「蘑菇账本」页面
2. 观察余额卡片中的蘑菇等级列表

**预期结果**：
1. 余额卡片显示全部五个等级：小蘑菇(2)、中蘑菇(0)、大蘑菇(0)、金蘑菇(0)、传说蘑菇(0)
2. 余额为 0 的等级仍正常显示图标和名称，数量显示「0」
3. 总积分显示「2」

- [ ] 通过
> 不通过时记录：
> - 实际结果：
> - 设备 / Android 版本 / App 版本：
> - 截图或日志：（可选）
> - Issue：#15

---

#### TC-B16 完成当日全部任务触发全勤奖并在进度卡中显示提示

**前置条件**：
- 当日共有 2 条任务，均未完成
- 当前中蘑菇余额为 M

**操作步骤**：
1. 进入任务列表，观察「今日进度」卡片底部提示文案
2. 完成第 1 条任务打卡
3. 完成第 2 条（最后一条）任务打卡
4. 进入「蘑菇账本」查看流水

**预期结果**：
1. 未全部完成时，进度卡显示「完成全部任务可额外获得 🍄‍🟫 中蘑菇×1」
2. 全部完成后，进度卡提示文案变为「🎉 已获得全勤奖 🍄‍🟫 中蘑菇×1」
3. 蘑菇账本流水中新增「+1 中蘑菇」，备注为「YYYY-MM-DD 全部任务完成奖励」
4. 中蘑菇余额变为 M+1

- [ ] 通过
> 不通过时记录：
> - 实际结果：
> - 设备 / Android 版本 / App 版本：
> - 截图或日志：（可选）
> - Issue：#16

---

#### TC-B17 今日进度卡显示连续打卡天数及下一里程碑提示

**前置条件**：
- 已连续打卡 5 天（今天是第 6 天）
- 当日有至少 1 条任务

**操作步骤**：
1. 进入任务列表
2. 观察「今日进度」卡片底部的连续打卡提示
3. 完成今日第 1 条任务打卡
4. 再次观察连续打卡提示（天数是否更新）

**预期结果**：
1. 进度卡显示「🔥 连续打卡 5 天 · 距 7 天里程碑还差 2 天」（或打卡后更新为 6 天，差 1 天）
2. 若连续天数已超过所有里程碑（7/30/100），显示「🔥 连续打卡 N 天 · 已达成全部里程碑！」
3. 打卡后天数即时刷新

- [ ] 通过
> 不通过时记录：
> - 实际结果：
> - 设备 / Android 版本 / App 版本：
> - 截图或日志：（可选）
> - Issue：#17

---

## v1.3.5 — 已完成任务禁止编辑 & 打卡历史页入口

**发布日期**：2026-03-04
**关联 Issue**：#13、#14
**关联提交**：`5c3385b`、`a82435a`
**修复范围**：
- #13：已完成任务卡片点击无反应，`TaskEditScreen` 进入只读模式（标题「查看任务（已完成）」，所有字段 disabled，保存/模板按钮隐藏）
- #14：任务列表页 TopBar 新增日历图标入口，`CheckInCalendarScreen` 补充返回按钮

---

#### TC-B13 已完成任务不可编辑，只读展示

**前置条件**：
- 当天有一条已完成的任务（打卡后状态为 DONE）

**操作步骤**：
1. 点击已完成任务卡片
2. 观察页面标题和字段状态
3. 尝试修改任务名称
4. 观察顶栏是否有「保存」按钮

**预期结果**：
1. 点击已完成任务卡片，页面**不跳转**（卡片无响应）
2. 已完成任务的卡片背景为灰色（surfaceVariant），视觉与未完成任务有区分
3. 任务卡片右侧仅显示删除按钮（无打卡按钮）

- [ ] 通过
> 不通过时记录：
> - 实际结果：
> - 设备 / Android 版本 / App 版本：
> - 截图或日志：（可选）
> - Issue：#13

---

#### TC-B14 打卡历史页可从任务列表进入

**前置条件**：
- 处于每日任务列表页

**操作步骤**：
1. 观察顶部标题栏右侧是否有日历图标按钮
2. 点击日历图标按钮
3. 观察进入的页面内容
4. 点击左上角返回按钮

**预期结果**：
1. 任务列表页顶栏右侧显示日历图标（📅）
2. 点击后进入打卡历史页，顶部显示当前月份（格式「YYYY年MM月」）
3. 页面包含：连续打卡卡片（当前连续 / 最长连续）、月历网格、星期标题行
4. 打卡日期有颜色标记，早完成日期显示 ⚡
5. 点击左上角返回按钮，正确返回任务列表

- [ ] 通过
> 不通过时记录：
> - 实际结果：
> - 设备 / Android 版本 / App 版本：
> - 截图或日志：（可选）
> - Issue：#14

---

## v1.3.4（待发版） — 提前奖励简化 & 打卡后顺序稳定

**发布日期**：待定
**关联 Issue**：#11、#12
**关联提交**：`a8aca5f`
**修复范围**：
- #11：`CheckInTaskUseCase` 移除按时间档计算提前奖励 fallback，未配置时统一给小蘑菇×1；`TaskEditScreen` 提示文案同步
- #12：`TaskDao.getTasksByDate` 排序由 `status, subject` 改为 `id`，打卡后任务不再移位

---

#### TC-B11 提前完成任务获得固定小蘑菇×1额外奖励

**前置条件**：
- 任务设置了截止时间，未自定义提前完成奖励（使用默认值）

**操作步骤**：
1. 在截止时间前点击打卡按钮
2. 观察奖励弹窗内容

**预期结果**：
1. 弹窗显示完成奖励（如「小蘑菇×1」）+ 提前完成奖励「提前完成 小蘑菇×1」
2. 不再显示「提前 XX 分钟」字样
3. 奖励数量固定为小蘑菇×1，与提前时长无关

- [ ] 通过
> 不通过时记录：
> - 实际结果：
> - 设备 / Android 版本 / App 版本：
> - 截图或日志：（可选）
> - Issue：#11

---

#### TC-B12 打卡完成后任务保持原始位置

**前置条件**：
- 当天有 3 条任务，顺序为 A、B、C，均未完成

**操作步骤**：
1. 记录当前顺序（A→B→C）
2. 点击任务 B 的打卡按钮完成
3. 观察任务列表顺序

**预期结果**：
1. 任务 B 变为已完成状态
2. 顺序仍为 A→B→C，B 不移位到末尾或首位
3. 继续完成其余任务后，顺序保持不变

- [ ] 通过
> 不通过时记录：
> - 实际结果：
> - 设备 / Android 版本 / App 版本：
> - 截图或日志：（可选）
> - Issue：#12

---

## v1.3.3（待发版） — 同一天庆祝横幅不再重复触发

**发布日期**：待定
**关联 Issue**：#10
**关联提交**：`d87612e`
**修复范围**：DailyTaskListScreen 新增本地 celebrationFiredDate 标记，防止 uiState 异步更新前 LaunchedEffect 重复触发横幅

---

#### TC-B10 同一天全部完成后第二次不再触发庆祝横幅

**前置条件**：
- 当天有若干任务，`celebrationShown` 为 false（当天尚未触发过横幅）

**操作步骤**：
1. 完成当天最后一条任务，观察庆祝横幅出现并 3 秒后消失
2. 新建一条新任务（使 isAllDone 变为 false）
3. 完成该新任务（使 isAllDone 再次变为 true）
4. 等待约 5 秒，观察是否出现第二次庆祝横幅

**预期结果**：
1. 步骤 1 后横幅正常出现并消失
2. 步骤 3 完成后，**不再**出现庆祝横幅
3. 任务列表正常显示全部完成状态，无横幅残留

- [ ] 通过
> 不通过时记录：
> - 实际结果：
> - 设备 / Android 版本 / App 版本：
> - 截图或日志：（可选）
> - Issue：#10

---

## v1.3.2（待发版） — 流水记录按年/日分组折叠展示

**发布日期**：待定
**关联 Issue**：#9
**关联提交**：`55142c5`
**修复范围**：MushroomLedgerScreen 流水记录改为年→日期两级分组，默认展开当年及今天，其余折叠

---

#### TC-B9 流水记录按年/日期分组，默认今天展开

**前置条件**：
- 蘑菇账本中有历史流水记录（至少今天有1条，其他日期也有记录）

**操作步骤**：
1. 进入「蘑菇」页面，查看流水记录区块
2. 观察年级头部（如「2026年」）和日期头部（如「今天 03月03日」）
3. 点击今天的日期头部，展开/折叠今天的记录
4. 点击年级头部，折叠/展开整年记录
5. 点击其他日期头部，展开该天记录

**预期结果**：
1. 流水记录按年降序排列，当年默认展开
2. 当年内，日期降序排列，今天默认展开，其余日期折叠
3. 今天日期头显示「今天 MM月dd日」，昨天显示「昨天 MM月dd日」
4. 每个头部右侧显示条数（如「3 条」）
5. 点击可展开/折叠，动作流畅，数据正确
6. 流水卡片时间戳只显示 HH:mm（日期已在组头显示）

- [ ] 通过
> 不通过时记录：
> - 实际结果：
> - 设备 / Android 版本 / App 版本：
> - 截图或日志：（可选）
> - Issue：#9

---

## v1.3.1（待发版） — 打卡后奖励预览始终显示

**发布日期**：待定
**关联 Issue**：#8
**关联提交**：`4ca7885`
**修复范围**：TaskCard 移除 !isDone 条件，打卡完成后奖励预览区域不再消失

---

#### TC-B8 打卡完成后奖励预览正常显示

**前置条件**：
- 当天任务列表中有一条未完成的任务

**操作步骤**：
1. 观察任务卡片底部奖励预览文字（打卡前）
2. 点击打卡按钮完成任务
3. 观察任务卡片底部区域

**预期结果**：
1. 打卡前：卡片底部显示预估奖励（如「🍄 小蘑菇×1」）
2. 打卡后：卡片底部仍显示奖励文字，内容为实际获得奖励（不消失）

- [ ] 通过
> 不通过时记录：
> - 实际结果：
> - 设备 / Android 版本 / App 版本：
> - 截图或日志：（可选）
> - Issue：#8

---

## v1.2.9（待发版） — 截止时间可编辑 & 状态同步 & 校验 Snackbar & 重复规则日期过滤修复
**关联 Issue**：#3、#4、#5
**关联提交**：`c8f0160`、`4be5973`、`1923734`
**修复范围**：截止时间新增时间选择器；DeadlineSection enabled 状态改为由 deadline != null 驱动；校验失败时触发 Snackbar 提示；自定义重复规则创建任务时过滤不符合规则的当天实例

---

#### TC-B3-1 截止时间可点击编辑

**前置条件**：
- 处于新建任务页

**操作步骤**：
1. 开启「截止时间」Switch
2. 点击显示的「截止：20:00」文字

**预期结果**：
1. 弹出时间选择器对话框，初始时间为 20:00
2. 修改时间后点击「确定」，文字更新为所选时间（如「截止：19:30」）
3. 点击「取消」，时间不变

- [x] 通过
> 不通过时记录：
> - 实际结果：
> - 设备 / Android 版本 / App 版本：
> - 截图或日志：（可选）
> - Issue：#3

---

#### TC-B3-2 开启截止时间后「自定义提前完成奖励」行正常显示

**前置条件**：
- 处于新建任务页，截止时间 Switch 默认关闭

**操作步骤**：
1. 打开「截止时间」Switch
2. 观察页面底部奖励设置区块

**预期结果**：
1. 奖励设置区块中出现「自定义提前完成奖励」行（含 Switch）
2. 关闭「截止时间」Switch 后，该行消失

- [x] 通过
> 不通过时记录：
> - 实际结果：
> - 设备 / Android 版本 / App 版本：
> - 截图或日志：（可选）
> - Issue：#3（同时覆盖 #6）

---

#### TC-B4 任务名称为空保存时显示 Snackbar 提示

**前置条件**：
- 处于新建任务页

**操作步骤**：
1. 不输入任务名称，直接点击右上角「保存」按钮

**预期结果**：
1. 页面不跳转，保持在新建任务页
2. 「任务名称 *」输入框下方显示红色错误文字「任务名称不能为空」
3. 底部 Snackbar 显示「请检查标红的必填项」

- [x] 通过
> 不通过时记录：
> - 实际结果：
> - 设备 / Android 版本 / App 版本：
> - 截图或日志：（可选）
> - Issue：#4

---

#### TC-B5 自定义重复规则创建任务时当天按规则过滤

**前置条件**：
- 当天为周二

**操作步骤**：
1. 进入新建任务页，输入任务名称「物理实验复习」，学科选「物理」
2. 选择重复规则「自定义」，勾选「周一」「周三」「周五」
3. 点击「保存」
4. 观察当天（周二）任务列表
5. 切换到周一，观察任务列表
6. 切换到周三，观察任务列表

**预期结果**：
1. 当天（周二）任务列表中不出现「物理实验复习」
2. 周一、周三任务列表中出现「物理实验复习」

- [x] 通过
> 不通过时记录：
> - 实际结果：
> - 设备 / Android 版本 / App 版本：
> - 截图或日志：（可选）
> - Issue：#5

---

## v1.3.0（待发版） — 模板页里程碑 Tab 返回状态恢复

**发布日期**：待定
**关联 Issue**：#7
**关联提交**：`20a3bef`
**修复范围**：模板页 selectedTab 改用 rememberSaveable，返回后恢复里程碑 Tab

---

#### TC-B7 模板页里程碑 Tab 返回后保持选中状态

**前置条件**：
- 处于模板页

**操作步骤**：
1. 点击「里程碑」Tab（index 2）
2. 点击「新建里程碑」进入里程碑创建页
3. 点击返回
4. 观察当前选中的 Tab

**预期结果**：
1. 返回后仍停留在「里程碑」Tab（index 2）
2. 「查看所有里程碑」→ 返回同样保持里程碑 Tab

- [x] 通过
> 不通过时记录：
> - 实际结果：
> - 设备 / Android 版本 / App 版本：
> - 截图或日志：（可选）
> - Issue：#7

---

## v1.2.7 — 复制任务日期选择器默认日期修复

**发布日期**：2026-03-03
**关联 Issue**：#2
**关联提交**：`bcbee25`
**修复范围**：复制任务时日期选择器默认选中当天而非明天（UTC+8 时区偏移 bug）

---

#### TC-B2 复制任务日期选择器默认选中明天

**前置条件**：
- 当日有至少 1 条 PENDING 任务
- 设备时区为 UTC+8（中国标准时间）

**操作步骤**：
1. 点击右下角 FAB（+）→「复制」
2. 观察弹出日期选择器顶部显示的选中日期，以及日历中哪一天有绿色填充圆

**预期结果**：
1. 顶部显示明天的日期（如今天是3月3日，则显示「2026年3月4日」）
2. 日历中明天（4日）有绿色填充圆，今天（3日）无填充圆

- [ ] 通过
> 不通过时记录：
> - 实际结果：
> - 设备 / Android 版本 / App 版本：
> - 截图或日志：（可选）
> - Issue：#2

---

## v1.2.5 — 2.2.1 任务设定三个遗留缺口修复

**发布日期**：2026-03-03
**关联提交**：`70967b2`
**修复范围**：任务描述字段 / 任务复制 UI / 自定义重复规则 UI

---

### Gap 1：任务描述字段

#### TC-1.1 新建任务时填写描述

**前置条件**：打开每日任务列表页

**操作步骤**：
1. 点击右下角 FAB（`+`）→「新建任务」进入编辑页
2. 在「任务名称」填写"数学作业"
3. 观察「任务名称」输入框下方是否出现「任务说明（选填）」多行文本框
4. 点击该文本框，输入"第12页第1-5题，注意写步骤"
5. 点击右上角「保存」

**预期结果**：
- 保存成功，返回任务列表，无报错
- 任务说明内容持久化（下一步 TC-1.2 验证）

- [x] 通过

---

#### TC-1.2 编辑已有任务时描述回显

**前置条件**：TC-1.1 已完成，"数学作业"已存在

**操作步骤**：
1. 点击「数学作业」任务卡片，进入编辑页
2. 观察「任务说明（选填）」文本框内容

**预期结果**：
- 文本框显示"第12页第1-5题，注意写步骤"（与保存时一致，不丢失）

- [x] 通过

---

#### TC-1.3 描述为空时正常保存

**操作步骤**：
1. 新建任务，填写标题，不填写任务说明
2. 点击「保存」

**预期结果**：
- 保存成功，无错误提示
- 重新进入编辑页，说明文本框为空（不显示乱码或 null）

- [x] 通过

---

#### TC-1.4 描述支持多行显示

**操作步骤**：
1. 新建任务，在「任务说明」输入 3 行以上内容（可用换行）
2. 保存后重新进入编辑页

**预期结果**：
- 输入框显示最少 2 行高度，超出内容可滚动（最多显示 4 行）
- 多行内容原样回显，换行保留

- [x] 通过

---

### Gap 2：任务复制 UI

#### TC-2.1 FAB 展开后显示「复制」按钮

**前置条件**：当日任务列表有至少 1 条 PENDING 任务

**操作步骤**：
1. 点击右下角主 FAB（`+`），不要立即点击
2. 观察展开后的次级按钮列表

**预期结果**：
- 展开后显示至少三个次级按钮：「模板」、「添加里程碑」、「复制」
- 各按钮标签清晰可见

- [x] 通过

---

#### TC-2.2 点击「复制」弹出日期选择器

**操作步骤**：
1. FAB 展开后点击「复制」小按钮

**预期结果**：
- FAB 收起
- 弹出系统日历样式的 DatePickerDialog，显示当前月份
- 对话框底部有「取消」和「复制」两个按钮

- [x] 通过

---

#### TC-2.3 选择目标日期完成复制

**前置条件**：今天有 2 条 PENDING 任务、1 条已完成任务

**操作步骤**：
1. 点击 FAB →「复制」，弹出日期选择器
2. 选择明天的日期
3. 点击「复制」确认

**预期结果**：
- 对话框关闭
- 底部 Snackbar 显示：「已复制 **2** 项任务到 \<明天日期\>」（只复制 PENDING，已完成不复制）
- 切换到明天日期（点击 TopBar 右箭头），确认两条任务已出现

- [ ] 通过
建议，复制的日期选择器，建议默认选中明天的日期，提升易用性。

> 不通过时记录：
> - 实际结果：的确默认选中了，但是选中的是当天，没有选中明天。
> - 设备 / Android 版本 / App 版本：1.2.6
> - 截图或日志：（可选）
> - Issue：#2
---

#### TC-2.4 取消日期选择不执行复制

**操作步骤**：
1. 点击 FAB →「复制」，弹出日期选择器
2. 选择任意日期后点击「取消」

**预期结果**：
- 对话框关闭，无 Snackbar，无任何任务被复制

- [x] 通过

---

#### TC-2.5 当日无任务时复制结果

**前置条件**：今天没有任何任务

**操作步骤**：
1. 点击 FAB →「复制」，选择目标日期，点击「复制」

**预期结果**：
- Snackbar 显示：「已复制 **0** 项任务到 \<目标日期\>」
- 目标日期无新增任务

- [x] 通过

---

### Gap 3：自定义重复规则 UI

#### TC-3.1 新建任务时选择「自定义」重复规则

**操作步骤**：
1. 进入新建/编辑任务页
2. 在「重复规则」区域观察选项列表

**预期结果**：
- 显示 4 个 RadioButton：不重复、每天、周一至周五、**自定义**
- 默认选中「不重复」

- [x] 通过

---

#### TC-3.2 选择「自定义」后显示星期 Checkbox

**操作步骤**：
1. 点击「自定义」RadioButton

**预期结果**：
- 「自定义」被选中（radio filled）
- 下方立即出现一行 7 个 Checkbox，标签分别为：周一、周二、周三、周四、周五、周六、周日
- 全部默认未勾选

- [x] 通过


---

#### TC-3.3 勾选指定星期并保存

**操作步骤**：
1. 选择「自定义」
2. 勾选「周一」「周三」「周五」
3. 填写任务标题后点击「保存」

**预期结果**：
- 保存成功，无报错
- 任务卡片上显示「🔄」重复标记

- [x] 通过

---

#### TC-3.4 编辑时自定义规则回显

**前置条件**：TC-3.3 已保存"周一、周三、周五"自定义规则任务

**操作步骤**：
1. 点击该任务卡片进入编辑页
2. 查看「重复规则」区域

**预期结果**：
- 「自定义」RadioButton 被选中
- 「周一」「周三」「周五」的 Checkbox 显示已勾选（✓）
- 「周二」「周四」「周六」「周日」未勾选

- [x] 通过

---

#### TC-3.5 切换回其他规则后 Checkbox 收起

**操作步骤**：
1. 在编辑页已选中「自定义」、Checkbox 可见
2. 点击「每天」RadioButton

**预期结果**：
- 「每天」被选中
- 星期 Checkbox 行立即消失（不再显示）
- 再次点击「自定义」，Checkbox 重新出现

- [x] 通过

---

#### TC-3.6 自定义规则不选任何星期时的边界行为

**操作步骤**：
1. 选择「自定义」但不勾选任何星期
2. 保存任务

**预期结果**：
- 保存成功（不强制校验）
- 该任务只在创建当日出现，之后无重复（等价于"不重复"，合理边界值）

- [x] 通过

---

### 快速回归核查清单

| #   | 检查点                                                | 结果  |
| --- | -------------------------------------------------- | --- |
| R-1 | 旧任务兼容性：升级后打开历史任务，描述字段为空但不崩溃                        | [ ] |
| R-2 | 备份兼容性：导出备份 JSON，确认 tasks 数组中每项有 `"description"` 字段 | [ ] |
| R-3 | 复制不含已完成：有完成任务的日期执行复制，目标日期只出现 PENDING 任务            | [ ] |
| R-4 | 自定义规则持久化：重启 App 后重新进入编辑页，自定义星期选择仍然正确               | [ ] |

---

## v1.4.8 — 里程碑输入框退格修复 & 拼图格子蘑菇等级区分

**发布日期**：2026-03-04
**关联 Issue**：#31、#32
**关联提交**：`7ee5a33`（#31）、`45d291f`（#32）
**修复范围**：里程碑分数段数字输入框退格遗留"1"；拼图格子按蘑菇等级显示不同 emoji

---

#### TC-B13 里程碑分数段输入框可完全退格清空并重新输入

**前置条件**：
- 处于「新建里程碑」页面
- 分数段配置区域已显示默认规则（含数量数字）

**操作步骤**：
1. 找到任意一档分数段的数量输入框（如显示「3」）
2. 连续按退格键，尝试将「3」删除到空
3. 重新输入「5」
4. 点击「保存里程碑」

**预期结果**：
1. 退格可将输入框完全清空（不会卡在"1"无法继续删除）
2. 重新输入「5」后显示「5」
3. 保存成功，使用修改后的数量

- [ ] 通过
> 不通过时记录：
> - 实际结果：
> - 设备 / Android 版本 / App 版本：
> - 截图或日志：（可选）
> - Issue：#31

---

#### TC-B14 拼图格子按兑换时选择的蘑菇等级显示不同 emoji

**前置条件**：
- 实物奖品「高达模型」已创建，拼图总块数 ≥ 5
- 学生同时拥有小蘑菇和金蘑菇余额

**操作步骤**：
1. 进入「高达模型」详情页
2. 在「消耗蘑菇」区域选择「金蘑菇（✨）」，数量 1，点击「确认兑换」，完成验证
3. 再次选择「小蘑菇（🍄）」，数量 1，点击「确认兑换」，完成验证
4. 观察拼图进度格子区域

**预期结果**：
1. 第一个解锁格子显示 ✨（金蘑菇 emoji）
2. 第二个解锁格子显示 🍄（小蘑菇 emoji）
3. 未解锁格子仍为灰色占位块，无 emoji

- [ ] 通过
> 不通过时记录：
> - 实际结果：
> - 设备 / Android 版本 / App 版本：
> - 截图或日志：（可选）
> - Issue：#32

---

## v1.4.11 — 奖品图片上传功能

**发布日期**：2026-03-04
**关联 Issue**：#20
**关联提交**：待填入
**修复范围**：创建奖品时可选择封面图片，列表卡片和详情页封面优先展示图片，无图降级到 emoji 占位符

---

#### TC-B15 创建奖品时可选择封面图片

**前置条件**：
- 以家长身份登录
- 设备相册中有至少一张图片

**操作步骤**：
1. 进入「奖品」页，点击右下角「+」
2. 观察创建页面顶部封面区域
3. 点击封面区域，系统相册弹出
4. 从相册选择一张图片
5. 观察封面区域
6. 填写奖品名称「测试奖品」，其余保持默认，点击「保存」
7. 回到奖品列表，找到「测试奖品」卡片
8. 点击进入详情页

**预期结果**：
1. 封面区域显示相机图标 + 「点击添加奖品图片（可选）」提示
2. 系统相册正常弹出
3. 图片加载后封面区域显示所选图片，底部有「点击更换图片」提示
4. 保存成功，返回列表
5. 列表卡片封面区域显示所选图片（而非 🎁 emoji）
6. 详情页封面区域同样显示所选图片

- [ ] 通过
> 不通过时记录：
> - 实际结果：
> - 设备 / Android 版本 / App 版本：
> - 截图或日志：（可选）
> - Issue：#20

---

#### TC-B16 未选择图片时封面降级显示 emoji

**前置条件**：
- 以家长身份登录

**操作步骤**：
1. 创建一个实物奖品，不选择任何图片，直接保存
2. 在列表中找到该奖品卡片，观察封面
3. 进入详情页，观察封面区域

**预期结果**：
1. 列表卡片封面显示 🎁 emoji（实物型）
2. 详情页封面显示 🎁 emoji（实物型）
3. 无图片时 emoji 正常居中显示，无异常

- [ ] 通过
> 不通过时记录：
> - 实际结果：
> - 设备 / Android 版本 / App 版本：
> - 截图或日志：（可选）
> - Issue：#20

---

## v1.4.12 — 统计页崩溃修复

**发布日期**：2026-03-04
**关联 Issue**：#1
**关联提交**：`abedfe5`
**修复范围**：消除 StatisticsScreen / CheckInTab / MushroomTab / ScoreTab 中提前 return 导致的 Compose slot table 不平衡崩溃

---

#### TC-B17 统计页正常加载不崩溃

**前置条件**：
- App 已安装 v1.4.12
- 有任意打卡或蘑菇数据（也可全空）

**操作步骤**：
1. 点击底部导航「统计」按钮
2. 等待数据加载完成
3. 点击「近7天」「近30天」「本学期」三个筛选器各一次
4. 点击「学习情况」「蘑菇收支」「成绩趋势」三个 Tab 各一次
5. 在「成绩趋势」Tab 中切换学科

**预期结果**：
1. 统计页正常打开，不崩溃
2. 各筛选器切换正常
3. 各 Tab 切换正常，无数据时显示「暂无...」提示
4. 切换学科正常

- [ ] 通过
> 不通过时记录：
> - 实际结果：
> - 设备 / Android 版本 / App 版本：
> - 截图或日志：（可选）
> - Issue：#1

---

## v1.4.13 — 拼图格子可见性修复 + 时长图标大小修复

**发布日期**：2026-03-04
**关联 Issue**：#25、#24
**关联提交**：`07893b1`
**修复范围**：拼图未解锁格子加边框可见；时长奖品图标改为 ⏰ 统一 44sp

---

#### TC-B18 拼图已解锁与未解锁格子有明显区分

**前置条件**：
- 实物型奖品已创建，已兑换 1 次以上（至少 1 块已解锁，总块数 > 已解锁数）

**操作步骤**：
1. 进入该实物奖品详情页
2. 查看「拼图进度」卡片中的格子区域

**预期结果**：
1. 已解锁格子：绿色（primary）背景 + 蘑菇 emoji
2. 未解锁格子：浅灰背景 + 描边轮廓，清晰可见（非隐身）
3. 格子总数 = 奖品设置的拼图总块数，不多不少

- [ ] 通过
> 不通过时记录：
> - 实际结果：
> - 设备 / Android 版本 / App 版本：
> - 截图或日志：（可选）
> - Issue：#25

---

#### TC-B19 时长奖品图标与实物奖品图标视觉大小相近

**前置条件**：
- 同时存在至少一个实物奖品和一个时长奖品

**操作步骤**：
1. 进入奖品列表页
2. 对比实物奖品卡片封面（🎁）和时长奖品卡片封面（⏰）

**预期结果**：
1. 两种 emoji 视觉大小相近，无明显偏小现象
2. 时长奖品显示 ⏰（闹钟），不再是旧的 ⏱（秒表）

- [ ] 通过
> 不通过时记录：
> - 实际结果：
> - 设备 / Android 版本 / App 版本：
> - 截图或日志：（可选）
> - Issue：#24

---

## v1.4.14 — 奖品封面图片自适应显示

**发布日期**：2026-03-04
**关联 Issue**：#20
**关联提交**：`24941e7`
**修复范围**：创建页/详情页/列表卡片封面图片改为 ContentScale.Fit，完整展示不裁切

---

#### TC-B20 奖品封面图片完整显示不被裁切

**前置条件**：
- 已为某奖品选择了封面图片（图片非正方形，有明显主体）

**操作步骤**：
1. 进入奖品列表页，找到有封面图片的奖品卡片
2. 观察卡片封面区域图片显示
3. 点击进入详情页，观察详情封面区域
4. 进入创建奖品页，选择一张图片后观察预览

**预期结果**：
1. 图片完整居中显示，无裁切，主体（如礼物/高达模型）完整可见
2. 图片与封面容器之间若有空隙，显示背景色（surfaceVariant）
3. 详情页和创建页预览均为完整图片

- [ ] 通过
> 不通过时记录：
> - 实际结果：
> - 设备 / Android 版本 / App 版本：
> - 截图或日志：（可选）
> - Issue：#20

---

## v1.4.1 — 今日进度卡显示备忘录连续完成天数提示

**发布日期**：2026-03-04
**关联 Issue**：#33
**关联提交**：`01859e9`
**修复范围**：DailyTaskListScreen — TaskProgressCard 新增备忘录连续天数提示行

---

#### TC-B21 今日进度卡显示备忘录连续完成天数

**前置条件**：
- 学生账户已连续完成「作业记录打卡」（HOMEWORK_MEMO）模板任务至少 1 天

**操作步骤**：
1. 进入每日任务列表页
2. 观察置顶「今日进度」卡片

**预期结果**：
1. 今日进度卡片中显示「📖 已连续完成备忘录任务 X 天 · 距 5 天里程碑还差 Y 天」
2. X 与实际连续天数一致，Y = 5 - X
3. 当 X ≥ 5 时，显示「📖 已连续完成备忘录任务 X 天 · 已达成里程碑！」
4. 当备忘录连续天数为 0（从未完成或中断）时，该行不显示

- [ ] 通过
> 不通过时记录：
> - 实际结果：
> - 设备 / Android 版本 / App 版本：
> - 截图或日志：（可选）
> - Issue：#33
